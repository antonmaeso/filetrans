spring:
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false   # don't auto-run jobs on startup

  datasource:
    url: jdbc:h2:mem:filetrans;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
